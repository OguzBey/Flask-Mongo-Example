# Flask-Mongo-Example

Flask + Mongo Example App

Install requirements

```
    pip install -r requirements.txt
```

Run

```
    python main.py
```
Endpoints:

- /register
- /users
- /users/{username}